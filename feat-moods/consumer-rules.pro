# Consumer ProGuard rules for the feat-moods module.
# Only keep rules belong here — global options like -printmapping/-printseeds/-printusage
# are not allowed in a consumer configuration and are emitted by the app module instead.
